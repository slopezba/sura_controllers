# Controllers Explained

The controller explanation for this package lives in [`README.md`](/home/cirtesu/cirtesub_ws/src/sura_controllers/README.md).

It covers:

- the `common`, `auv`, and `usv` controller layout
- ROS 2 Control plugin discovery
- topic mode and chained mode
- each controller's role
- debug and realtime notes
- build instructions from the workspace root
